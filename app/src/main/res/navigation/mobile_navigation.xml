<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/nav_studi">

    <fragment
        android:id="@+id/nav_rank"
        android:name="com.ilkom.quizkita.ui.rank.RankFragment"
        android:label="@string/title_rank"
        tools:layout="@layout/fragment_rank" />

    <fragment
        android:id="@+id/nav_studi"
        android:name="com.ilkom.quizkita.ui.studi.StudiFragment"
        android:label="@string/title_studi"
        tools:layout="@layout/fragment_studi" />

    <fragment
        android:id="@+id/nav_profile"
        android:name="com.ilkom.quizkita.ui.profile.ProfileFragment"
        android:label="@string/title_profile"
        tools:layout="@layout/fragment_profile" />
</navigation>